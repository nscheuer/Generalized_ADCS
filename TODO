- Check camelcase or spacings
- Estimators do not use J2000
- Estimator Consistence of using P and Q
- Make MTQ_w_RW that also uses the magnetotorquers for rotation, not just for desaturation
- General Disturbance Missing

Controllers
- All current controllers do gyroscopic precession compensation, but no disturbance torque compensation
Packaging / PyPI (deferred from the 0.1.8 install-hygiene work, 2026-08-05)
- Ship py.typed (PEP 561). Currently absent, so every downstream type checker
  must discard our annotations. Blocked on cleaning the annotations first:
  ~240 mypy errors across the package, incl. ~54 implicit-Optional defaults
  (`def f(x: int = None)`). Shipping the marker before that pushes our errors
  into users' type checking. Fix the annotations, then add the marker.
- requirements.txt is still a hand-maintained pinned environment that can drift
  from pyproject.toml (the junk `np==1.0.2` is gone and the file now documents
  its role, but nothing enforces consistency). CI installs it and never runs
  `pip install .`, so the pyproject dependency set is exercised only by the
  wheel smoke-test in publish.yml. Consider having CI install `.[dev]` instead.
- Add a TestPyPI dry-run to the release process. `twine check` only validates
  metadata rendering; it does not catch a wheel that installs but cannot
  import (which is how 0.1.5 shipped without cholesky_update.py). The wheel
  smoke-test job in .github/workflows/publish.yml covers most of this now.
- Decide whether to yank 0.1.5 (PEP 592). Its SRUAKF hard-fails at first use
  because it lazily imports choldate, which has no PyPI release. Yanking keeps
  it installable by exact pin but removes it from resolution. Left un-yanked
  for now since 0.1.5 works for everything except the SRUAKF.
