{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Debug C++ Extension",
            "type": "cppdbg",
            "request": "launch",
            // IMPORTANT: Point to the Python interpreter in your venv
            "program": "${workspaceFolder}/venv/bin/python", 
            // IMPORTANT: Point to the Python script running your test
            "args": ["${workspaceFolder}/papers/3MTQ+1RW/debug_altro_bc2_3+1.py"], 
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb", // <--- ADD THIS LINE
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}